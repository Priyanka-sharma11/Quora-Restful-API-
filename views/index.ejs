<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>All Posts</title>

</head>
<body>
    <h1>Quora Posts</h1>
    <% for(post of posts){ %>
        <div class="post">
            <h3 class="user"> @<%= post.username  %></h3> 
            <h4 class="content"><%= post.content  %></h4> 
            <button><a href="http://localhost:8080/posts/<%=post.id%>">See in Detail</a></button>
            <button><a href="http://localhost:8080/posts/<%=post.id%>/edit">Edit</a></button><br>
           <form method="post" action="/posts/<%=post.id%>?_method=DELETE">
                <button class="dlt-btn">Delete Post</button>
            </form>
        </div>
    <% } %>

    <br><br>
    <a href="http://localhost:8080/posts/new" class="clr-btn">Create new Post</a>
</body> 
</html>